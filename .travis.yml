language: go

go:

  - "1.19.6"


group: bluezone

matrix:
  fast_finish: true
  allow_failures:
  - go: tip

cache:
  bundler: true

sudo: true

before_install:
  - sudo apt-get install bc

before_script:
  - make fmt

script:
  - make container
