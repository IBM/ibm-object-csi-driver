language: go

go:
  - "1.20.1"

group: bluezone

matrix:
  fast_finish: true
  allow_failures:
  - go: tip

cache:
  bundler: true

sudo: true

before_script:
  - make fmt
  - make lint

script:
  - make container
