language: go

go:

  - "1.19.12"


group: bluezone

matrix:
  fast_finish: true
  allow_failures:
  - go: tip

cache:
  bundler: true

sudo: true

before_script:
  - make fmt
  - make lint

script:
  - make driver
